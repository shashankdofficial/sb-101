package q3;

public class Statement {

	//it if to use our database
	//It is good if we want to execute a query only once.
	//we cannot execute it with parameters at runtime.
	//It throws the SQLExeption.
	//It will slow down the performance.
}
